# 清宵皮肤包注册补丁：把它的公开 dsh.client 条目插入 web 插件列表。
# 本包即官方 dsh 插件形态，用
# `dsh plugin --profile <name> add <路径或 git URL>` 安装即可；
# 皮肤之间的互斥由 home 层的 disabled 行管理（与其他 ui 皮肤一致）。
- insert:
    - id: ui-skin-qingxiao
      name: '@dsh-external/dsh-client-ui-skin-qingxiao'
